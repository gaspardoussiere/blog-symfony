{% extends "base.html.twig" %}

{% block body %}
    <h1>{{ category.title }} ({{category.posts | length}} articles)</h1>
    {% for post in category.posts %}
        <h2>{{ post.title }}</h2>
    {% endfor %}
{% endblock %}
